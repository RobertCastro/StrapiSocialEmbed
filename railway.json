{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build:plugin && npm run build"
  },
  "deploy": {
    "startCommand": "npm start",
    "healthcheckPath": "/admin",
    "healthcheckTimeout": 300
  }
}
